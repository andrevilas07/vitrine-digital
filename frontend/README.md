# LetsMuse Shop — Frontend

Front-end em React (Vite) para o e-commerce de moda feminina simplificado. Consome a API do backend Laravel em `../backend`.

## Rodando o projeto

```
npm install
npm run dev
```

A URL da API é configurada em `.env` (`VITE_API_URL`).
